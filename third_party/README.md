# 第三方源码与工具

此目录默认忽略外部检出内容，只提交本说明和 `manifest.example.json`。大型源码/模型优先保持在外部已有目录，通过私有机器配置引用。

需要某个依赖时，记录上游 URL、固定 commit/version、许可及用途。许可证允许且明确需要纳入版本时，再审阅为精确的 ignore 例外或 Git submodule；不提交完整 `.venv`、缓存或下载的模型权重。项目代码的 MIT 许可不替代第三方资产/SDK 的许可。

不把这里当 Python 安装目录：仿真和导出环境分别由 uv 管理，最终锁文件等待实际运行时兼容性验证后生成。
