# B_SCENE — 双臂装配与相机标定

后续指定模型：GPT-6 Luna Max（2026-09-23升级；以下历史执行不追溯变更）；2026-09-22 本轮实际启动并交付原子手/相机链、失败用例和装配输入模板，主会话已审阅集成。消费主会话批准拓扑包，实测值仍缺失，未通过 M2。详见 `docs/ASSEMBLY_INPUTS.md`。

## 输入
先读 AGENTS.md、本任务相关 DESIGN 章节和最小所需 manifest；不要整库重复检索。缺少实际输入时提交缺失清单，不能填猜测值。

## 允许修改
packages/sim2data_isaac/src/sim2data/backends/isaaclab/scene*, assets/owned/mounts/, tests/test_frames*

公共 schema、依赖锁、main 与其他代理目录归负责人。

## 工作
消费主会话/Astra 已批准的观察包（观察 ID、私有来源/哈希、帧或时间、审阅者、结论和不可推断量），结合实测/现有标定装配双臂、手、转接总成、下置腕相机和固定主相机。Luna 不负责独立得出视觉结论。先提交参数清单；有实际数据后建立完整 SE(3) 链和相机光学约定适配。4 cm 只作未标定名义长度。Blender 只补缺失部件。三路 RGB 名称已确认，腕部流配置和全部位姿仍需冻结；depth 当前关闭。主相机型号可设计，但必须填写位姿并核查覆盖，手机绕拍视角不充当训练视角。

## 验收
场景可打开；坐标标记/标定板投影正确；无初始穿透；相机安装总成不会随错误的 finger link 运动；输出三视角或实际已确认视角截图，未安装相机不得伪造。

## 返回
不超过 500 字摘要：变更、已运行测试、未运行项、关键证据路径、阻塞。完整记录放测试报告。只有真实运行和真实提交后才报告结果或 SHA。
