# Sim2Data 协作约束

## 目标与当前阶段

AIRBOT Play + OmniHand 2025 双臂桌面接力，Isaac Lab 主线，LeRobotDataset v3.0。用户于 2026-09-22 确认：左臂从桌面左侧抓盒→桌面中转区放下、松手、撤离→右臂重新抓起→放入桌外右侧框子。整条链为一个 episode，两段全部成功才计成功。

两只手下均安装 D405；主相机允许采用通用仿真相机。约 4 cm 法兰长度是名义估计，完整装配变换和惯性待核。现在处于 M0/M1 前置阶段，生产模型、标定和运行环境未绑定。不要把纯 Python 单元测试通过称为物理抓取或数据集兼容性通过。

**当前阶段：2026-09-22 用户已授权下一阶段 A/B/D 实施及有条件的运行时验证。** 先审阅旧 worktree，再在正式发布历史派生的独立实施分支推进资产/模型审计、装配输入与官方 SDK synthetic smoke。GPU 空间和依赖闭包允许时执行最小 headless physics + RGB。A/B 未验收前不启动 C，E 保留在途计划器，不扩正式批量采集。CPU、运行时 smoke 和格式小样均不等于生产验收。用户另授权 synthetic 装配假设、独立 uv 环境、本机 headless 和及时双远端 push；Astra Max 已实际完成装配专项复核。生产实测参数继续为 null，装配未验收前不采集接力。

## 协调与模型

主会话负责人拥有架构、公共 schema、坐标/单位约定、版本选择和最终合并权。将窄而清晰的实现任务交给 **GPT-6 Luna Max**。用户于2026-09-23指定后续使用 `gpt-6-luna` 与 `max` reasoning；实际运行前检查本地客户端/账号是否支持该组合。不可静默替换成其他模型，也不可声称未实际启动的子代理已完成工作。

视频、图像、装配关系、遮挡和其他多模态理解由**主会话或 GPT-6 Astra High**负责；主会话先输出观察结论、证据位置、不可从画面推断的量，再交 Luna 实现小任务。Luna 可执行抽帧、整理元数据及确定性几何运算，不负责最终视觉结论或通过视觉猜生产标定。

视觉任务交接采用批准观察包：observation ID、私有来源标识/哈希、帧或时间位置、主会话/Astra 审阅者、观察结论、已确认用户说明、不可推断量和批准状态。派生帧路径及原始哈希记录放 ignored 私有证据目录。未批准的观察不得作为 Luna 的建模事实；图像不能提供的米制尺寸/姿态继续留空。

GPT-6 Sol High（`gpt-6-sol`）可承担边界明确的复杂适配/代码复核；GPT-6 Astra High 可承担多模态及架构复核。二者按实际需要启用并记录，不把所有任务升级为高成本模型。旧会话的5.6支持记录不证明新模型可用；新模型必须在实际启动前核对支持，不以模板或清单代替实际启动记录。

`.codex/config.toml.example` 及 `.codex/agents/` 是配置模板，不是子代理已被本聊天启动的记录。不要覆盖用户已有 Codex 配置；先审阅合并。默认最多 3 个子线程并发，按依赖分两波完成 5 类任务；不要递归生成子代理，也不要用更多线程替代明确的任务拆分。

子代理只读本任务所需文件和 manifest，不反复读全仓库、不回传大段日志/模型全文、不各自下载一套仿真环境。省的是重复上下文与高成本模型使用，不承诺多代理会降低总 token 数。交付限于变更文件、测试命令、关键证据、阻塞项和下一接口；短摘要建议不超过 500 字，完整证据存文件。

## 工作区与并行写

每个代理使用独立分支/worktree，限定允许修改的目录。不得并发修改 uv.lock、公共 scene schema、同一个 USD、同一个导出目录。公共接口变更先交由负责人裁决。共享官方资产只读；每个代理独立临时目录；公共 cache 不手工修改。

## 禁止事项

- 不访问或控制真机 CAN/电机，不发送运动指令；本项目阶段是仿真。
- 不自行升级 GPU 驱动/系统包、不 sudo 安装、不改全局 bashrc、不删除现有环境。
- 不下载完整官方资产库、不把大文件嵌入代码/对话、不伪造资产路径或关节数。
- 不把用户实录、内网路径、标定原件、凭据或未授权第三方资产提交到公有 GitHub。
- 不对物体粘手/瞬移或关闭必要碰撞来伪造成功抓取。
- 不手拼 LeRobot v3 元数据冒充已验证格式，不让多个 writer 共写一个数据根目录。
- 不用视觉估计覆盖实测标定；null 的生产参数必须被解决或阻断采集。
- 不 force push、不直接合并 main、不静默重写 Git remote。

## 交付格式

每个 task 返回：输入版本和假设；变更文件；运行过的准确测试命令及结果；未运行项目；剩余阻塞；提交 SHA（仅实际提交后填写）。没有 GPU、NAS、SDK 或权限时，只交付可测试部分并注明缺口。交付附实际效果图与图例；装配修正优先提供前后对比、左右腕部近景，并标注静态/物理验证范围。私有实录和第三方派生图不因此获得公开发布授权。

## 执行顺序

当前实施：主会话维护三份文档、验证记录与公共 schema；先核对环境、资源和已有在途实现，再推进已授权 A/B/D。发现跨文件矛盾时一并修改文档、草案与对应回归测试。旧未合入实现继续保留各自 worktree，新实施分支从正式发布历史派生，审阅后按文件复用。

后续实现第一波：资产/模型审计 A、相机/装配 B、导出 D 可并行。A 先锁来源和驱动映射；B 先落实主会话已复核的布局与参数清单；D 先明确官方 SDK 版本与记录接口。需要新增大依赖时先盘点已有环境。

后续验证：A/B 具备真实输入后再做场景打开、投影和碰撞检查；D 用官方 SDK 做仅用于格式的小样写入/回读。通过 A/B 后才实现接触任务 C，先验证两段再串完整接力。E 可先做 plan-only seed/划分框架，M3/M4 通过后才扩大随机化及批量跑测。

公共边界：A 输出资产/控制 manifest；B 输出坐标链与装配需求；C 输出同步状态、实际命令与阶段证据；D 消费冻结的 frame/schema 并 finalize/回读；E 消费验收结果而不修改成功判据。公共 schema 变更先由主会话批准，生产参数缺失时保持采集关闭。

详见 tasks/A_ASSETS.md 至 tasks/E_QA_BATCH.md。

2026-09-23 模型协议升级：后续指定GPT-6 Luna Max / GPT-6 Sol High，替代5.6系列相应角色。历史已执行代理记录不追溯改写。项目模板已更新，不覆盖用户全局配置；客户端若未暴露指定模型，主会话继续工作并记录限制，禁止静默退回5.6。
