[project]
name = "sim2data-foundation"
version = "0.1.0"
description = "Dependency-free preflight and contracts for AIRBOT Play + OmniHand simulation data"
requires-python = ">=3.10"
dependencies = []

[tool.uv]
package = false
